import 'package:flutter/material.dart';

import '../../../shared/feature_placeholder_screen.dart';

class StudentApplicationsScreen extends StatelessWidget {
  const StudentApplicationsScreen({super.key});

  @override
  Widget build(BuildContext context) => const FeaturePlaceholderScreen(title: 'Applications');
}
